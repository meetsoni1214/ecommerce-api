apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "ecommerce-api.fullname" . }}-config
  labels:
    {{- include "ecommerce-api.labels" . | nindent 4 }}
data:
  PORT: {{ .Values.config.port | quote }}
  AZURE_STORAGE_CONTAINER: {{ .Values.config.azureStorageContainer | quote }}
